@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Register";
}
@* ✅ DISABLED — see ExternalLoginModel.cs. Every handler on this page now
   redirects to /Account/Login before Page() is ever returned, so this
   markup never actually renders. Left as a stub in case external login is
   rebuilt against the JWT API in the future. *@
<p>External login is not available.</p>
