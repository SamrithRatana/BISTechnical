@page
@model ExternalLoginsModel
@{
    ViewData["Title"] = "Manage your external logins";
    ViewData["ActivePage"] = ManageNavPages.ExternalLogins;
}
@* ✅ DISABLED — see ExternalLoginsModel.cs. External login management is not
   compatible with this app's JWT-based auth and every handler now redirects
   away, so this markup (which read Model.CurrentLogins / Model.OtherLogins)
   would never actually render. Left as a stub in case the page is ever
   re-enabled against a JWT-aware external login flow. *@
<p>External login management is not available.</p>
